const size = {
  sm: '640px',
  md: '768px',
  lg: '1024px',
  xl: '1280px'
}

export const breakpoints = {
  sm: `(mid-width: ${size.sm})`,
  md: `(mid-width: ${size.md})`,
  lg: `(mid-width: ${size.lg})`,
  xl: `(mid-width: ${size.xl})`
}